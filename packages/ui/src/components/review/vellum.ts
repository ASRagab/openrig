// CORRECTIVE §7.4 — the ONE vellum card recipe for the review family.
// Near-background color with partial transparency + backdrop blur: the page's
// dot grid reads as faintly DIFFUSED behind the card, never sharp — a layered
// vellum panel you notice sitting above the paper as you scroll. Token-driven,
// so both themes hold with no per-theme edits (--background flips with .dark).
export const VELLUM_CARD =
  "border border-outline-variant bg-[hsl(var(--background)/0.82)] backdrop-blur-[2px]";
